apiVersion: apps/v1
kind: Deployment
metadata:
  name: vival-agent
  namespace: vival
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vival-agent
  template:
    metadata:
      labels:
        app: vival-agent
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: agent
          image: ghcr.io/isaksamsten/voice-grader/agent:main
          env:
            - name: NODE_ENV
              value: "production"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: vival-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              value: "redis://redis:6379"
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: vival-secrets
                  key: OPENAI_API_KEY
            - name: LIVEKIT_URL
              valueFrom:
                secretKeyRef:
                  name: vival-secrets
                  key: LIVEKIT_URL
            - name: LIVEKIT_API_KEY
              valueFrom:
                secretKeyRef:
                  name: vival-secrets
                  key: LIVEKIT_API_KEY
            - name: LIVEKIT_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: vival-secrets
                  key: LIVEKIT_API_SECRET
            - name: DATA_DIR
              value: "/data"
          volumeMounts:
            - name: app-data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
        - name: app-data
          hostPath:
            path: /data0/vival/data
            type: DirectoryOrCreate
